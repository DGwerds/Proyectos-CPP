#include <iostream>
#include <windows.h>

using namespace std;

main(){
	int n;
	cout << "Ingrese un numero tamano del vector: ";
	cin >> n;
	
	int vector[n];
	
	cout << "Ingrese numeros del vector: ";
	for (int i = 0; i < n; i++){
		cin >> vector[i];
	}
	
	bool esPrimo = false;
	for (int i = 0; i < n; i++){
		for (int j = 0; j < vector[i]; j++){
			if(i != 1 || i != ){
				
			}
		}
	}
	
	cout << "";
}

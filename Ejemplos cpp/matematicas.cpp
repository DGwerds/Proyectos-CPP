#include <iostream>
#include <math.h>

using namespace std;

main(){										//no hay que explicar mucho en este codigo
	int n;
	cout << "Ingrese un numero entero: ";
	cin >> n;
	cout<< n << " multiplicado por dos es: " << n*2 << "\n";
	cout<< n << " menos 1 es: " << n-1 << "\n";
	cout<< "La raiz cuadrada de " << n << " es: " << sqrt(n) << "\n"; //SQuare RooT(numero)
	cout<< "El valor absoluto de " << n << "-10 es: " << abs(n-10) << "\n"; //ABSolute(numero)
}
